
h1 MongoDB-Viewer: Collections

div#collections
  - if (collections)
    ul
      - each collection in collections
        - if (collection)
          li
            a(href="/database/#{db_name}/collections/#{collection.name}/")
              #{collection.name}
              span(class="count") #{collection.count}