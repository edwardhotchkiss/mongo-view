<h1>MongoDB Viewer: <%- collectionName %></h1>

<div id="collections">
  <% if (collection && collection.length > 0) { %>
    <ul>
      <% collection.forEach(function(item) { %>
        <li><a href="#">$<%= item._id %></></li>
      <% }); %>
    </ul>
  <% } %>
</div>